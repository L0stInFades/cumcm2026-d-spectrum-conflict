# 数据说明（附件 1）

探查方式：`python3 tools/cli.py exec --file <脚本>`，读取 ingest 阶段的 `data/附件1__Sheet1.parquet`。

## 结构

- 150 行，列：用频装备编号、频段区间、时间区间、间隔时长、使用次数；无缺失，编号唯一，区间均为 `[lo,hi)` 半开整数区间。
- 类别由编号首字母给出：A 类 20 个、B 类 40 个、C 类 90 个，与简报一致。
- 每类参数完全同质：

| 类别 | 频宽 w | 时长 d | 间隔 g | 次数 n | 周期 d+g | 单计划时间跨度 | 单计划单元数 |
|---|---|---|---|---|---|---|---|
| A | 10 | 5 | 60 | 3 | 65 | 135 | 150 |
| B | 15 | 3 | 40 | 4 | 43 | 132 | 180 |
| C | 3 | 2 | 8 | 12 | 10 | 112 | 72 |

- 首次时间起点 s：0…531（均值 259.6，近似均匀）；频段起点 f：0…96，`f+w ≤ 100` 全部满足。
- 最晚结束时刻（原始计划）：643 Δt；时频区域 100 × 643 = 64300 个单元，被占用 14698 个（22.9%），其中 1842 个单元被 ≥2 个计划占用，最大重叠 4。
- 各类占用单元：A 3000、B 7200、C 6480。

## 结果模板（附件 2）

| 文件 | 表头（A1 之后由本仓库按模板原文填写） |
|---|---|
| result1.xlsx | 序号、冲突装备1、冲突设备2（模板原文如此，保留） |
| result2.xlsx | 用频装备编号、调整后频段区间、调整后时间区间、是否撤销用频计划 |
| result3.xlsx | 新增用频装备序号、调整后频段区间、调整后时间区间 |
| result4.xlsx | 用频装备编号、调整后频段范围、调整后时间区间、调整后间隔时长、是否撤销用频计划 |

## 与简报的差异

无。简报中的参数、数量与数据一致；简报未给出的量（起点分布、时间范围 643、占用率）记录如上。

## 热启动提示文件（configs/hints/）

`q2_decisions.json`、`q4_decisions.json`：run `20260912-102234-ea48b61` 的 `resolve` / `resolve_interval` 阶段经独立校验的现任解（目标向量分别为 (0,0,12,16,37,71,750) 与 (0,0,9,13,36,78,776)；在该 run 结束后回写，使下一次运行从本轮的最好解出发），仅记录 (编号, 动作, 幅度) 三元组与来源信息（目标向量、幅度上限）。它们只作为 CP-SAT/HiGHS 的搜索起点（提示），不构成约束；`resolve`/`resolve_interval` 的报告与数字（`QtwoHintFeasible`、`QtwoHintVector` 等）记录提示是否可行及其目标向量。不提供提示时流水线仍可运行，只是现任解可能更差（MDR-0008）；自 MDR-0009 起，求解器在每级加入由现任解导出的上界割，因此报告向量在词典序上**永不劣于提示**——这也意味着本轮结果可由本仓库的提示文件直接复现。
